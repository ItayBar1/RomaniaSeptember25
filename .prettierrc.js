module.exports = {
    // מגביל את מספר התווים בשורה אחת
    printWidth: 120,

    // כמה רווחים לטאב
    tabWidth: 4,

    // להשתמש ברווחים במקום טאבים
    useTabs: false,

    // להוסיף נקודה-פסיק בסוף כל שורה
    semi: true,

    // להשתמש בגרשיים בודדים (' ') במקום כפולים (" ")
    singleQuote: true,

    // להשאיר רווחים בתוך סוגריים מסולסלים { example: true }
    bracketSpacing: true,

    // מיקום הסוגר הסוגר של פונקציות JSX באותה שורה
    jsxBracketSameLine: false,

    // פסיק בסוף אובייקטים ומערכים (ES5-compatible)
    trailingComma: 'es5',

    // שמירה על שבירת שורות בקוד HTML
    htmlWhitespaceSensitivity: 'css',

    // שמירה על סוגריים סביב פרמטרים בפונקציות חץ
    arrowParens: 'always',
};
